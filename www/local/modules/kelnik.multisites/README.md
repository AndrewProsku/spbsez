Multisite (bitrix module)
====================

Add to composer.json

```
{
     "require": {
         "kelnik/module-multisites": "^3"
     },
     "repositories": [
         {
             "type": "git",
             "url":  "git@gitlab.kelnik.pro:estate/kelnik.module.multisites.git"
         }
     ]
 }
 ```