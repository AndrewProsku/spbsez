{% if id %}
    <div class="b-form__checkbox">
        <input
            id="{{ id }}"
            class="b-checkbox-input"
            type="checkbox"
            value="{{ value }}"
            name="{{ name }}"
            {% if checked %} checked {% endif %}
            {% if disabled %} disabled {% endif %}
            {% if required %} required {% endif %}
        >
        <label for="{{ id }}" class="b-checkbox-label">
            <span class="b-checkbox-box">{% include './check.svg.twig' %}</span>
            <span class="b-checkbox-text">{{ text }}</span>
        </label>
    </div>
{% else %}
    <p>Пожалуйста, укажи уникальный id для инпута</p>
{% endif %}
